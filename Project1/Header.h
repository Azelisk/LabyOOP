#pragma once
#include <iostream>
using namespace std;
struct fraction
{
	double first;
	double second;
	void Init(double, double);
	void Write();
	void Show();
	double schet(double x1, double y1);
};
